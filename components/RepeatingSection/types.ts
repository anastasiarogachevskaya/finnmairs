// types.ts or in your component file
export type SectionProps = {
  imageUrl: string;
  text: string;
  background: string;
  isImageRight: boolean; // true for image on the right, false for left
  svgPatternUrl: string;
};
